# Ren'Py 개발 팁

## 실시간 코드 수정 및 테스트

Ren'Py는 개발 중에 매우 유용한 핫 리로드 기능을 제공합니다.

### 주요 단축키

#### 1. **Shift+R (리로드)**
- 게임 실행 중에 코드를 수정한 후 `Shift+R`을 누르면 게임을 재시작하지 않고 스크립트를 다시 로드합니다
- 현재 진행 중인 장면에서 바로 적용됩니다
- **가장 많이 사용하는 기능입니다!**

#### 2. **Shift+D (개발자 메뉴)**
- 개발자 도구 메뉴를 엽니다
- 다음과 같은 기능 제공:
  - 변수 감시 (Variable Viewer)
  - 이미지 위치 조정 (Image Location Picker)
  - 스타일 인스펙터
  - 씬 리스트

#### 3. **Shift+O (콘솔)**
- 파이썬 콘솔을 열어서 변수 값을 확인하거나 수정할 수 있습니다
- 예: `game_points`, `player_name` 같은 변수를 실시간으로 확인/수정

#### 4. **Shift+E (에디터로 이동)**
- 현재 실행 중인 스크립트 라인으로 에디터를 열어줍니다

#### 5. **Shift+I (이미지 인스펙터)**
- 화면에 표시된 이미지들을 확인할 수 있습니다

### 권장 작업 흐름

```
1. Ren'Py 게임 실행
2. 코드 편집기에서 .rpy 파일 수정
3. Shift+R을 눌러서 리로드
4. 바로 수정사항 확인
5. 필요시 2-4 반복
```

### 주의사항

#### 리로드가 적용되는 경우
- 대화문 수정
- 선택지 변경
- 라벨 내용 수정
- 대부분의 스크립트 로직 변경

#### 게임 재시작이 필요한 경우
- `init` 블록의 변경사항
- `define` 문으로 정의한 상수 변경
- `config` 설정 변경
- 새로운 이미지나 오디오 파일 추가
- 캐릭터 정의 변경
- 트랜지션이나 변환(transform) 정의 변경

### 추가 개발 팁

#### 빠른 스킵
- `Ctrl` 키를 누르고 있으면 텍스트를 빠르게 스킵할 수 있습니다
- `Tab` 키로도 스킵 모드를 토글할 수 있습니다

#### 롤백
- 마우스 휠을 위로 올리거나 `Page Up` 키로 이전 대화로 돌아갈 수 있습니다
- 개발 중 특정 선택지나 장면을 다시 테스트할 때 유용합니다

#### 세이브/로드
- `F5`: 빠른 저장
- `F9`: 빠른 불러오기
- 특정 시점을 반복 테스트할 때 유용합니다

### 디버깅 팁

#### 콘솔에서 변수 확인
```python
# Shift+O를 눌러 콘솔을 열고
print(변수명)
dir()  # 현재 정의된 모든 변수 확인
```

#### 조건부 디버그 메시지
```renpy
# 개발 중에만 표시되는 메시지
if config.developer:
    "디버그: 현재 점수는 [game_points]입니다."
```

### 퍼포먼스 프로파일링
- `Shift+D` → "Performance" 메뉴에서 게임의 성능을 분석할 수 있습니다
- FPS, 메모리 사용량 등을 확인할 수 있습니다

---

## 참고 자료
- [Ren'Py 공식 문서](https://www.renpy.org/doc/html/)
- [Ren'Py 한국어 위키](https://renpy.kr/)

